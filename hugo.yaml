baseURL: https://ali-a404.github.io/
languageCode: en-us
title: Absentia
contentDir: content

markup:
  highlight:
    noclasses: true
    style: "github"
  # Skip renderer safety to load HTML in codespace
  goldmark:
    renderer:
      unsafe: true

module:
  imports:
    path: "github.com/willfaught/paige"

# theme: ["PaperMod"]